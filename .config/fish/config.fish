function fish_user_key_bindings
  bind \cg peco_select_ghq      # Ctrl-g
  bind \ch peco_select_history  # Ctrl-h
  bind \cj peco_select_z        # Ctrl-j
  bind \cf peco_select_file     # Ctrl-f
end

# Updating the theme according to the dark theme in macos
update_theme

# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
# Comentando momentaneamente
#eval /Users/kevv87/opt/anaconda3/bin/conda "shell.fish" "hook" $argv | source

# <<< conda initialize <<<

